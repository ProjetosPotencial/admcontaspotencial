diff --git a/app/(app)/caixa-entrada/caixa-entrada-client.tsx b/app/(app)/caixa-entrada/caixa-entrada-client.tsx
index 7894f44..e748e25 100644
--- a/app/(app)/caixa-entrada/caixa-entrada-client.tsx
+++ b/app/(app)/caixa-entrada/caixa-entrada-client.tsx
@@ -16,6 +16,7 @@ type Item = {
   requerente?: string | null;
   destinatario_detectado?: string | null; destinatario_cnpj_detectado?: string | null;
   chave_acesso?: string | null;
+  conta_existente_id?: string | null;
   beneficiario?: string | null;
   fornecedor_detectado?: string | null; cnpj_detectado?: string | null;
   numero_documento_detectado?: string | null;
@@ -288,6 +289,36 @@ export default function CaixaEntradaClient({ itens: itensIniciais, lojas }: { it
     setTimeout(() => setToast(null), 5000);
   }
 
+  async function substituirLancamento(item: Item) {
+    if (!item.conta_existente_id) return;
+    setProcessando(item.id);
+    const contaId = item.conta_existente_id;
+    // atualiza os dados da NF na conta existente
+    await supabase.from("contas").update({
+      numero_nf: item.numero_documento_detectado ?? null,
+      remetente_cnpj: item.cnpj_detectado ?? null,
+      destinatario_razao: item.destinatario_detectado ?? null,
+      destinatario_cnpj: item.destinatario_cnpj_detectado ?? null,
+      chave_acesso: item.chave_acesso ?? null,
+    }).eq("id", contaId);
+    // atualiza o valor do último lançamento e registra histórico
+    const { data: lancs } = await supabase.from("lancamentos").select("id").eq("conta_id", contaId).order("lancado_em", { ascending: false }).limit(1);
+    if (lancs?.[0]?.id) {
+      if (item.valor_detectado != null) await supabase.from("lancamentos").update({ valor: item.valor_detectado }).eq("id", lancs[0].id);
+      const { data: { user } } = await supabase.auth.getUser();
+      await supabase.from("lancamento_historico").insert({
+        lancamento_id: lancs[0].id, de: "—",
+        para: item.numero_documento_detectado ? `NF ${item.numero_documento_detectado}` : "dados atualizados",
+        comentario: `Substituição por reenvio do chamado ${item.chamado_numero ?? ""}`.trim(),
+        quem: user?.id ?? null, em: new Date().toISOString(),
+      });
+    }
+    await supabase.from("caixa_entrada_boletos").update({ status: "confirmado", revisado_em: new Date().toISOString() }).eq("id", item.id);
+    setItens((lista) => lista.filter((i) => i.id !== item.id));
+    setToast("Lançamento substituído com sucesso.");
+    setProcessando(null);
+  }
+
   async function rejeitar(item: Item) {
     setProcessando(item.id);
     const { data: { user } } = await supabase.auth.getUser();
@@ -318,7 +349,8 @@ export default function CaixaEntradaClient({ itens: itensIniciais, lojas }: { it
             {chamados.map((item) => (
               <ChamadoCard key={item.id} item={item} lojas={lojas} processando={processando === item.id}
                 onConfirmarChamado={confirmarChamado} onRejeitar={rejeitar}
-                onEditado={(id, patch) => setItens((lista) => lista.map((i) => (i.id === id ? { ...i, ...patch } : i)))} />
+                onEditado={(id, patch) => setItens((lista) => lista.map((i) => (i.id === id ? { ...i, ...patch } : i)))}
+                onSubstituir={substituirLancamento} />
             ))}
             {boletos.length === 0 && chamados.length === 0 ? (
               <div className="card text-center py-10 text-[13px] text-[#adb5bd]">Nenhum boleto esperando revisão.</div>
@@ -352,11 +384,12 @@ export default function CaixaEntradaClient({ itens: itensIniciais, lojas }: { it
 
 type PreviaNF = { empresa: string };
 
-function ChamadoCard({ item, lojas, processando, onConfirmarChamado, onRejeitar, onEditado }: {
+function ChamadoCard({ item, lojas, processando, onConfirmarChamado, onRejeitar, onEditado, onSubstituir }: {
   item: Item; lojas: Loja[]; processando: boolean;
   onConfirmarChamado: (item: Item, lojaId: string) => void;
   onRejeitar: (item: Item) => void;
   onEditado: (id: string, patch: Partial<Item>) => void;
+  onSubstituir: (item: Item) => void;
 }) {
   const [lojaId, setLojaId] = useState("");
   const [busca, setBusca] = useState("");
@@ -512,10 +545,23 @@ function ChamadoCard({ item, lojas, processando, onConfirmarChamado, onRejeitar,
       })()}
 
       <div className="flex gap-2 justify-end">
-        <button onClick={() => onConfirmarChamado(item, lojaId)} disabled={!lojaId || processando}
-          className="btn-primario disabled:opacity-40">
-          {processando ? "Lançando..." : "Lançar compra"}
-        </button>
+        {item.conta_existente_id ? (
+          <>
+            <button onClick={() => onSubstituir(item)} disabled={processando}
+              className="btn-primario disabled:opacity-40">
+              {processando ? "Substituindo..." : "Substituir existente"}
+            </button>
+            <button onClick={() => onConfirmarChamado(item, lojaId)} disabled={!lojaId || processando}
+              className="btn-secundario disabled:opacity-40" title={!lojaId ? "Escolha a loja para criar novo" : ""}>
+              Criar novo
+            </button>
+          </>
+        ) : (
+          <button onClick={() => onConfirmarChamado(item, lojaId)} disabled={!lojaId || processando}
+            className="btn-primario disabled:opacity-40">
+            {processando ? "Lançando..." : "Lançar compra"}
+          </button>
+        )}
         <button onClick={() => onRejeitar(item)} disabled={processando} className="btn-secundario">Descartar</button>
       </div>
     </div>
diff --git a/lib/importar-caixa-entrada.ts b/lib/importar-caixa-entrada.ts
index 7c105b6..a071c2a 100644
--- a/lib/importar-caixa-entrada.ts
+++ b/lib/importar-caixa-entrada.ts
@@ -330,50 +330,46 @@ export async function importarCaixaEntradaDrive() {
               existente = r.data;
             }
 
+            // chamado_pasta_id sintético por NF, pra não colidir no índice único
+            const idSintetico = `${ch.pastaId}#${ext.chave_acesso ?? ext.numero_documento ?? Math.random().toString(36).slice(2)}`;
+            const baseCard = {
+              drive_web_view_link: `https://drive.google.com/drive/folders/${ch.pastaId}`,
+              classe_documento: "chamado",
+              chamado_numero: ch.chamadoNumero,
+              chamado_pasta_id: idSintetico,
+              drive_file_id: idSintetico,
+              chamado_rotulo: ch.lojaTexto,
+              requerente: dadosGlpi?.requerente ?? null,
+              tipo_detectado: "compra",
+              fornecedor_detectado: ext.fornecedor ?? null,
+              cnpj_detectado: ext.cnpj ?? null,
+              destinatario_detectado: ext.destinatario ?? null,
+              destinatario_cnpj_detectado: ext.destinatario_cnpj ?? null,
+              chave_acesso: ext.chave_acesso ?? null,
+              numero_documento_detectado: ext.numero_documento ?? null,
+              emissao_ano: ext.data_emissao?.ano ?? null,
+              emissao_mes: ext.data_emissao?.mes ?? null,
+              emissao_dia: ext.data_emissao?.dia ?? null,
+              valor_detectado: ext.valor ?? null,
+              confianca: "media",
+            };
+
             if (existente) {
-              // NF já cadastrada → atualiza os dados da conta + histórico
-              await supabase.from("contas").update({
-                numero_nf: ext.numero_documento ?? null,
-                remetente_cnpj: ext.cnpj ?? null,
-                destinatario_razao: ext.destinatario ?? null,
-                destinatario_cnpj: ext.destinatario_cnpj ?? null,
-                chave_acesso: ext.chave_acesso ?? null,
-              }).eq("id", existente.id);
-              const { data: lancs } = await supabase.from("lancamentos").select("id").eq("conta_id", existente.id).order("lancado_em", { ascending: false }).limit(1);
-              if (lancs?.[0]?.id) {
-                await supabase.from("lancamento_historico").insert({
-                  lancamento_id: lancs[0].id, de: "—",
-                  para: ext.numero_documento ? `NF ${ext.numero_documento}` : "dados atualizados",
-                  comentario: `Atualização por reenvio do chamado ${ch.chamadoNumero ?? ""}`.trim(),
-                  quem: null, em: new Date().toISOString(),
-                });
-              }
+              // NF já existe → NÃO atualiza sozinho. Cria um card pra o usuário
+              // escolher: substituir o lançamento existente ou criar um novo.
+              await supabase.from("caixa_entrada_boletos").insert({
+                ...baseCard,
+                nome_arquivo: `Reenvio · Chamado ${ch.chamadoNumero ?? "?"} · NF ${ext.numero_documento ?? ""} (já existe)`.trim(),
+                conta_existente_id: existente.id,
+                duplicada: true,
+                observacao: `Atenção: essa NF já está lançada. Escolha substituir ou criar novo (chamado ${ch.chamadoNumero ?? ""}).`.trim(),
+              });
               reenviosAtualizados++;
             } else {
-              // NF nova → cria card na Caixa (revisão manual), vinculado ao mesmo chamado.
-              // chamado_pasta_id sintético por NF, pra não colidir no índice único.
-              const idSintetico = `${ch.pastaId}#${ext.chave_acesso ?? ext.numero_documento ?? Math.random().toString(36).slice(2)}`;
+              // NF nova → card normal de reenvio, para revisão e lançamento
               await supabase.from("caixa_entrada_boletos").insert({
-                drive_file_id: idSintetico,
+                ...baseCard,
                 nome_arquivo: `Reenvio · Chamado ${ch.chamadoNumero ?? "?"} · NF ${ext.numero_documento ?? ""}`.trim(),
-                drive_web_view_link: `https://drive.google.com/drive/folders/${ch.pastaId}`,
-                classe_documento: "chamado",
-                chamado_numero: ch.chamadoNumero,
-                chamado_pasta_id: idSintetico,
-                chamado_rotulo: ch.lojaTexto,
-                requerente: dadosGlpi?.requerente ?? null,
-                tipo_detectado: "compra",
-                fornecedor_detectado: ext.fornecedor ?? null,
-                cnpj_detectado: ext.cnpj ?? null,
-                destinatario_detectado: ext.destinatario ?? null,
-                destinatario_cnpj_detectado: ext.destinatario_cnpj ?? null,
-                chave_acesso: ext.chave_acesso ?? null,
-                numero_documento_detectado: ext.numero_documento ?? null,
-                emissao_ano: ext.data_emissao?.ano ?? null,
-                emissao_mes: ext.data_emissao?.mes ?? null,
-                emissao_dia: ext.data_emissao?.dia ?? null,
-                valor_detectado: ext.valor ?? null,
-                confianca: "media",
                 observacao: `Reenvio: nova NF do chamado ${ch.chamadoNumero ?? ""}`.trim(),
               });
               reenviosNovos++;
